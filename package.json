{
  "name": "clawpr",
  "version": "1.0.0",
  "description": "GitHub repo janitor for the OpenClaw ecosystem. Scan issues, submit clean PRs, integrate security.",
  "author": "SkunkWorks0x",
  "license": "MIT",
  "type": "module",
  "bin": {
    "clawpr": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npx tsx --test tests/*.test.ts",
    "start": "node dist/cli.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.0.0",
    "@types/node": "^22.0.0"
  }
}
